#include<stdio.h>
void main()
{
	int a=15,b=17,c=25;
	if(a==b&&b==c)
		printf("tringle is equivilant");
	 else
	  if(a==b||b==c||a==c)
	   printf("tringle is isoscalen") ;
	   else 
	   if(a!=b!=c)
	     printf("tringle is scalen");
}
